# Uncomment the next line to define a global platform for your project
# platform :ios, '11.0'

target 'iOS chat application' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for iOS chat application
    pod 'CLTypingLabel', '~> 0.4.0'
    pod 'Firebase/Auth'
    pod 'Firebase/Firestore'

  target 'iOS chat applicationTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'iOS chat applicationUITests' do
    # Pods for testing
  end

end
